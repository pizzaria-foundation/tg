# What `../SDK/tools/symrelease` needs to know about this repository.

OWNER=pizzaria-foundation
REPO=tg

# The application is the repository root here: app.conf sits beside Cargo.toml rather than under
# apps/, because this repository has exactly one and never grew the directory.
BUILD="."

SHIP="build/telegram.sis"
